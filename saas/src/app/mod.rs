//! Application modules.
//!
//! Each domain module has its own `model.rs` and `routes.rs`.
//! Routes annotated with `#[route]` are auto-discovered — no manual registration needed.

// Example: after running `floz generate scaffold User name:string email:string`
// pub mod user;
